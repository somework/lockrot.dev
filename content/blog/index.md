---
# `title` names the page in the navigation and in the breadcrumbs, so it stays short; `head_title`
# is the one that goes into <title> and the OpenGraph title (see overrides/main.html).
# Both long values are quoted: an unquoted YAML scalar cannot contain ": ".
title: Blog
head_title: "lockrot blog — dependency rot and Composer internals"
description: "Notes from building lockrot: measured runs over real composer.lock files, and what composer audit --abandoned misses in 31 PHP projects."
---

# Blog
